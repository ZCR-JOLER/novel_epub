# Python
__pycache__/
*.py[cod]
*.egg-info/

# 运行产物（本书仓库不存放任何抓取的正文/书籍）
data/
output/
debug/

# 自动生成的配置：config.py 由 auto.py/gui 每次运行自动重写，不入库；
# 模板请看 config.example.py（需要手动改结构时复制一份为 config.py）
config.py
config.py.bak

# 日志 / 临时文件
*.log

# 虚拟环境
.venv/
venv/

# 打包产物（PyInstaller 等）
dist/
build/
*.spec

# 系统文件
.DS_Store
Thumbs.db
